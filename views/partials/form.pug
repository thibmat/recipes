form(method="post")
    .form-group
        label(for='name') Nom
        input#name.form-control(type='text', placeholder='Nom', name="name" value=(recipe)?recipe.name:'').form-group
    .form-group
        label(for='intro') Introduction
        textarea#intro.form-control(rows='3', name="intro")
            if(recipe)
                | #{recipe.intro}
    .form-group
        label(for='nbIngredients') Nombre d'ingrédients
        input#nbIngredients.form-control(type='number', name="nbIngredients" value=(recipe)?recipe.nbIngredients:'').form-group
    h3 Ingrédients de la recette
    #ingredients
    if(update)
        input(type="hidden" name="_method" value="put")
    button.btn.btn-primary(type='submit')
        if(update)
            | Modifier
        else
            | Créer

